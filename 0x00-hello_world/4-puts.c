#include <stdio.h>

/**
 * main - prints tis clause "Programming is like building a multilingual puzzle
 * Return: 0 if no syntax erro but non-zero if otherwise
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
        return (0);
}
